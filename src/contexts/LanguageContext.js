import React from "react";

export default React.createContext("english");

// Here just to see when we console log it what we find inside it
// const lang = React.createContext("english");

// console.log(lang);

// export default lang;
